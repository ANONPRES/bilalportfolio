# FaceHarmony AI frontend

Next.js 15 + TypeScript client for educational facial geometry analysis.

## Develop

```bash
npm install
cp .env.example .env.local
npm run dev
```

Requires the FastAPI backend at `NEXT_PUBLIC_API_URL` (default `http://localhost:8000`).
